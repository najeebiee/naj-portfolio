# Portfolio fonts

Add the licensed variable font files here:

- `clash-display-variable.woff2`
- `satoshi-variable.woff2`

The global stylesheet already declares both font faces and provides system
fallbacks while the local files are absent.
